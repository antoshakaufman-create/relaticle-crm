# 🚀 Финальные инструкции по развертыванию

## ✅ Все готово к запуску!

Все файлы подготовлены и настроены. Следуйте инструкциям ниже.

## 📋 Шаг 1: Подключение к серверу

```bash
ssh root@83.220.175.224
# Пароль: YOUR_ADMIN_PASSWORD
```

## 📋 Шаг 2: Клонирование репозитория

```bash
cd /var/www
git clone <ВАШ_РЕПОЗИТОРИЙ_GIT> relaticle
cd relaticle
```

**Важно:** Замените `<ВАШ_РЕПОЗИТОРИЙ_GIT>` на URL вашего Git репозитория.

Если репозиторий еще не создан:
1. Создайте репозиторий на GitHub/GitLab
2. Загрузите код в репозиторий
3. Используйте URL репозитория в команде выше

## 📋 Шаг 3: Запуск развертывания

### Вариант A: Автоматический (рекомендуется)

```bash
chmod +x run_deployment.sh
./run_deployment.sh
```

### Вариант B: Ручной запуск

```bash
chmod +x deploy.sh

ADMIN_NAME="Администратор" \
ADMIN_EMAIL="YOUR_ADMIN_EMAIL" \
ADMIN_PASSWORD="YOUR_ADMIN_PASSWORD" \
YANDEX_GPT_API_KEY="YOUR_YANDEX_GPT_API_KEY" \
YANDEX_FOLDER_ID="YOUR_YANDEX_FOLDER_ID" \
DB_TYPE=sqlite \
./deploy.sh
```

## ⏱️ Время развертывания

Процесс займет примерно **10-15 минут**, в зависимости от скорости интернета и сервера.

## ✅ После завершения

1. **Откройте в браузере:**
   ```
   http://lizon0707.fvds.ru/sysadmin
   ```

2. **Войдите с учетными данными:**
   - Email: `YOUR_ADMIN_EMAIL`
   - Пароль: `YOUR_ADMIN_PASSWORD`

3. **Проверьте работу:**
   - Создайте команду (Team)
   - Проверьте настройки
   - Протестируйте поиск лидов

## 🔒 Настройка SSL (рекомендуется)

После успешного развертывания настройте SSL сертификат:

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d lizon0707.fvds.ru
```

После этого приложение будет доступно по HTTPS.

## 🔍 Проверка работы

```bash
# Статус сервисов
systemctl status nginx
systemctl status php8.4-fpm
systemctl status relaticle-queue

# Логи приложения
tail -f /var/www/relaticle/storage/logs/laravel.log

# Логи Nginx
tail -f /var/log/nginx/error.log
```

## 🆘 Решение проблем

### Проблема: "Permission denied"
```bash
chmod +x deploy.sh
chmod +x run_deployment.sh
```

### Проблема: "Class Redis not found"
Проверьте `.env` файл - должно быть:
```
CACHE_STORE=database
```

### Проблема: "Database connection failed"
Проверьте права доступа:
```bash
chown -R www-data:www-data /var/www/relaticle/database
chmod 664 /var/www/relaticle/database/database.sqlite
```

### Проблема: "502 Bad Gateway"
Проверьте PHP-FPM:
```bash
systemctl status php8.4-fpm
systemctl restart php8.4-fpm
```

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи: `tail -f /var/www/relaticle/storage/logs/laravel.log`
2. Проверьте статус сервисов
3. Очистите кеш: `php artisan config:clear && php artisan cache:clear`

---

**Готово к запуску! Удачи!** 🎉

