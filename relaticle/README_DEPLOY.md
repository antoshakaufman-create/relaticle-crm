# 🚀 Развертывание Relaticle CRM - Готово!

## ✅ Все подготовлено

Все файлы и скрипты готовы к развертыванию на сервере FirstVDS.

## 📋 Быстрый старт

### Вариант 1: Автоматическое развертывание (рекомендуется)

1. **Подключитесь к серверу:**
   ```bash
   ssh root@83.220.175.224
   # Пароль: YOUR_ADMIN_PASSWORD
   ```

2. **Клонируйте репозиторий:**
   ```bash
   cd /var/www
   git clone <ваш-репозиторий> relaticle
   cd relaticle
   ```

3. **Запустите скрипт развертывания:**
   ```bash
   chmod +x deploy.sh
   
   ADMIN_NAME="Администратор" \
   ADMIN_EMAIL="YOUR_ADMIN_EMAIL" \
   ADMIN_PASSWORD="YOUR_ADMIN_PASSWORD" \
   YANDEX_GPT_API_KEY="YOUR_YANDEX_GPT_API_KEY" \
   YANDEX_FOLDER_ID="YOUR_YANDEX_FOLDER_ID" \
   DB_TYPE=sqlite \
   ./deploy.sh
   ```

Скрипт автоматически:
- ✅ Установит PHP 8.4, Composer, Node.js, Nginx
- ✅ Настроит базу данных SQLite
- ✅ Настроит .env с YandexGPT ключами
- ✅ Выполнит миграции
- ✅ Настроит Nginx
- ✅ Создаст администратора
- ✅ Настроит systemd сервисы

### Вариант 2: Использование готовых команд

Скопируйте команды из файла `DEPLOY_COMMANDS.txt` и выполните их на сервере.

## 📁 Подготовленные файлы

- **`deploy.sh`** - Автоматический скрипт развертывания
- **`DEPLOY_COMMANDS.txt`** - Готовые команды для копирования
- **`DEPLOYMENT_ENV.txt`** - Переменные окружения для .env
- **`DEPLOY_NOW.md`** - Подробная инструкция
- **`QUICK_DEPLOY.md`** - Краткая инструкция
- **`DEPLOYMENT_STEPS.md`** - Пошаговая инструкция

## 🔐 Данные для входа

После развертывания:

- **URL**: `http://lizon0707.fvds.ru/sysadmin`
- **Email**: `YOUR_ADMIN_EMAIL`
- **Пароль**: `YOUR_ADMIN_PASSWORD`

## ⚙️ Настройка SSL (опционально)

После развертывания настройте SSL сертификат:

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d lizon0707.fvds.ru
```

## 🔍 Проверка работы

```bash
# Статус сервисов
systemctl status nginx
systemctl status php8.4-fpm
systemctl status relaticle-queue

# Логи
tail -f /var/www/relaticle/storage/logs/laravel.log
```

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи: `tail -f /var/www/relaticle/storage/logs/laravel.log`
2. Проверьте статус сервисов: `systemctl status <service-name>`
3. Очистите кеш: `php artisan config:clear && php artisan cache:clear`

---

**Готово к развертыванию!** 🎉

