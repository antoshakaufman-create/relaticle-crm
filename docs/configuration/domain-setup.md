# Инструкция по привязке своего домена

## 🌐 Настройка DNS записей

### Вариант 1: Поддомен (рекомендуется)

Если вы хотите использовать поддомен, например `crm.вашсайт.ru`:

1. **Зайдите в панель управления вашего домена** (где вы покупали домен)

2. **Добавьте A-запись:**
   - **Тип:** A
   - **Имя/Поддомен:** `crm` (или любое другое)
   - **Значение/IP:** IP адрес вашего сервера (например, IP сервера lizon0707.fvds.ru)
   - **TTL:** 3600 (или оставьте по умолчанию)

3. **Подождите 5-30 минут** для распространения DNS записей

4. **Проверьте DNS запись:**
   ```bash
   dig crm.вашсайт.ru
   # или
   nslookup crm.вашсайт.ru
   ```

### Вариант 2: Основной домен

Если вы хотите использовать основной домен `вашсайт.ru`:

1. **Добавьте A-запись:**
   - **Тип:** A
   - **Имя/Поддомен:** `@` или оставьте пустым (зависит от панели)
   - **Значение/IP:** IP адрес вашего сервера
   - **TTL:** 3600

---

## 🔧 Обновление конфигурации на сервере

### 1. Обновите Nginx конфигурацию

```bash
nano /etc/nginx/sites-available/relaticle
```

Измените строку `server_name`:

```nginx
server_name вашсайт.ru crm.вашсайт.ru;
```

### 2. Обновите .env файл

```bash
cd /var/www/relaticle
nano .env
```

Измените:

```env
APP_URL=https://crm.вашсайт.ru
```

### 3. Перезагрузите Nginx

```bash
nginx -t
systemctl reload nginx
```

### 4. Обновите SSL сертификат

```bash
certbot --nginx -d вашсайт.ru -d crm.вашсайт.ru
```

Или если сертификат уже установлен:

```bash
certbot --nginx -d вашсайт.ru -d crm.вашсайт.ru --expand
```

### 5. Очистите кеш Laravel

```bash
cd /var/www/relaticle
php artisan config:clear
php artisan config:cache
```

---

## ✅ Проверка

1. **Откройте в браузере:** `https://crm.вашсайт.ru`
2. **Проверьте SSL сертификат** (должен быть зелёный замочек)
3. **Проверьте, что сайт открывается** без ошибок

---

## 🔍 Решение проблем

### DNS не обновился

- Подождите до 48 часов (обычно 5-30 минут)
- Проверьте правильность IP адреса
- Убедитесь, что TTL не слишком большой

### SSL сертификат не устанавливается

- Убедитесь, что домен указывает на правильный IP
- Проверьте, что порты 80 и 443 открыты в фаерволе
- Проверьте логи: `tail -f /var/log/nginx/error.log`

### Сайт не открывается

- Проверьте конфигурацию Nginx: `nginx -t`
- Проверьте логи: `tail -f /var/log/nginx/error.log`
- Убедитесь, что сервисы запущены: `systemctl status nginx php8.4-fpm`

---

## 📝 Примеры популярных DNS провайдеров

### Cloudflare
1. Зайдите в Dashboard → DNS
2. Добавьте A-запись: `crm` → IP адрес
3. Убедитесь, что Proxy выключен (серый облачко) для A-записи

### Яндекс.Коннект
1. Зайдите в раздел "Домены" → "DNS-записи"
2. Добавьте A-запись: `crm` → IP адрес

### REG.RU / Timeweb / другие
1. Зайдите в панель управления доменом
2. Найдите раздел "DNS-записи" или "Управление DNS"
3. Добавьте A-запись с поддоменом `crm` и IP адресом

---

## 🔐 Дополнительная безопасность

После настройки домена рекомендуется:

1. **Настроить регулярные бэкапы**
2. **Включить мониторинг** (например, UptimeRobot)
3. **Настроить fail2ban** для защиты от брутфорса
4. **Обновлять систему** регулярно: `apt update && apt upgrade -y`

